{
  "name": "muktarquest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./public/dist && npx webpack && cp -r ./dist ./public",
    "serve": "npx webpack serve",
    "docs": "cp -r ./public ./docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kaboom": "^3000.0.2"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "webpack": "^5.84.1",
    "webpack-cli": "^5.1.1",
    "webpack-dev-server": "^4.15.0"
  }
}
